const styles = (theme) => ({
  footerRoot: {
    alignSelf: 'flex-end',
    width: '100vw',
    backgroundColor: theme.palette.green.dark,
    display: 'grid',
    gridTemplateColumns: '1fr 1fr 1fr 1fr',
    padding: '20px 30px',
  },
});

export default styles;
